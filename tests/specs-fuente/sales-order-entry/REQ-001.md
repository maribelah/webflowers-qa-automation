# REQ-001 — Ingreso al módulo Sales Order Entry desde el menú principal

## Prompt original

@workspace /crear-test
Módulo: Sales Order Entry
Requerimiento: REQ-001
Funcionalidad: Ingreso al módulo Sales Order Entry desde el menú principal
Pasos del flujo exitoso:
  1. Navegar a la URL del ambiente activo
  2. Ingresar usuario y contraseña desde el JSON de datos
  3. Hacer clic en el botón de ingresar
  4. Validar que carga el Dashboard
  5. Hacer clic en el menú Sales usando el localizador //div[@class='ellipsis selected-parent' and text()='Sales']
  6. Hacer clic en el submenú New usando el localizador //ul[@id='subSales']/li[2]/div/div
  7. Hacer clic en la opción Order Entry usando el localizador //div[@title='Sales - Order Entry']
  8. Validar que se abre la ventana Sales-Order Entry
Resultado esperado: El sistema abre la ventana Sales-Order Entry correctamente
Validación BD: No aplica
Page Class disponible: Crear SalesOrderEntryPage.ts
Datos de prueba disponibles: Crear REQ-001-data.json

---

## Análisis UI y localizadores

### Pantallas involucradas
- Login
- Dashboard
- Navegación de menú principal
- Ventana Sales-Order Entry

### Elementos y localizadores clave
- **Menú Sales:**
  - XPath: `//div[@class='ellipsis selected-parent' and text()='Sales']`
- **Submenú New:**
  - XPath: `//ul[@id='subSales']/li[2]/div/div`
- **Opción Order Entry:**
  - XPath: `//div[@title='Sales - Order Entry']`
- **Contenedor ventana Sales-Order Entry:**
  - Sugerido: `[data-testid='sales-order-entry']`, `.sales-order-entry`, `//h2[contains(text(),'Sales-Order Entry')]`

### Flujo esperado
1. Login exitoso (usuario y contraseña desde JSON)
2. Validación de Dashboard visible
3. Navegación por menú lateral:
   - Click en Sales
   - Click en New
   - Click en Order Entry
4. Validar apertura de ventana Sales-Order Entry

### Notas
- Todos los selectores deben declararse como `readonly` en la Page Class.
- Usar preferencia: `data-testid` > `role` > `text` > `css` > `xpath`.
- Si no existe `data-testid`, mantener el XPath como fallback.
- Screenshots en cada paso crítico.
- No requiere validación de base de datos.

---

*Generado automáticamente por Copilot QA Agent — Mayo 2026*